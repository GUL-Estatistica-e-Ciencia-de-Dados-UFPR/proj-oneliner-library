{
  "id": "journalctl-errors-today",
  "title": "Show system executables with errors/failures from today's journal",
  "language": "bash",
  "category": "system-monitoring",
  "command": "echo \"=== Errors/Failures in Today's Journal ===\"; journalctl --no-pager --since today --grep 'fail|error|fatal' --output json | jq -r '._EXE' | sort | uniq -c | sort -nr | awk '{printf \"%4d  %s\\n\", $1, $2}'",
  "description": "Lists system executables that have generated failure, error, or fatal messages in today's journal logs, sorted by frequency.",
  "explanation": "The command retrieves all journal entries from today (since midnight), filters for messages containing the keywords 'fail', 'error', or 'fatal' (case-insensitive regex), outputs the results in JSON format for parsing, extracts the executable name (_EXE field) from each log entry, counts occurrences of each executable, sorts them numerically in descending order (most frequent first), and formats the output with a header and aligned columns for readability.",
  "tags": [
    "journalctl",
    "systemd",
    "logs",
    "monitoring",
    "troubleshooting",
    "errors",
    "failure",
    "system-administration",
    "diagnostics",
    "debugging",
    "logging",
    "bash",
    "jq",
    "text-processing",
    "process-management"
  ],
  "author": "marcos",
  "created_at": "2026-04-22",
  "updated_at": "2026-04-22",
  "safety": "safe",
  "shell": "bash",
  "platforms": [
    "linux",
    "gnu-linux"
  ],
  "dependencies": [
    "systemd",
    "jq"
  ],
  "arguments": [],
  "examples": [
    {
      "command": "journalctl-errors-today",
      "description": "Show errors/failures from today's journal"
    },
    {
      "command": "journalctl --no-pager --since yesterday --grep 'fail|error|fatal' --output json | jq -r '._EXE' | sort | uniq -c | sort -nr",
      "description": "Show errors/failures from yesterday's journal (raw output)"
    }
  ],
  "output": "=== Errors/Failures in Today's Journal ===\n   5  /usr/bin/systemd\n   3  /usr/bin/foo-daemon\n   1  /usr/lib/bar-service",
  "notes": [
    "Requires journalctl (systemd) and jq to be installed",
    "Only shows entries from the current day (midnight to now)",
    "Requires appropriate permissions to read journal logs",
    "The regex pattern 'fail|error|fatal' matches any of these words in log messages",
    "Output is sorted by count (highest first) for easy identification of frequent issues"
  ],
  "see_also": [
    "dmesg-errors-pretty",
    "user-activity-and-quota-report"
  ],
  "status": "reviewed"
}
